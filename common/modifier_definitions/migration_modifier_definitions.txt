# Author(s): AngriestBird
# DO NOT USE THIS ONE: LAW ONLY
base_migration_rate_value = {
	color_type = good
	value_type = percentage
	precision = 2
	category = country
}

# DO NOT USE THIS ONE: LAW ONLY -- Used for Law Display
maximum_migration_rate_value = {
	color_type = good
	value_type = percentage
	precision = 2
	category = country
}

migration_rate_value_factor = {
	color_type = good
	value_type = percentage
	precision = 2
	category = country
}
