categories = {

	infastructure_strikes = {
		intel_source = civilian

		available = {
			OR = {
				NOT = {
					has_idea = intervention_local_security
					has_idea = intervention_isolation
				}
				has_war = yes
			}

		}
	}

	military_resource_raids = {
		intel_source = army

		available = {
			OR = {
				NOT = {
					has_idea = intervention_local_security
					has_idea = intervention_isolation
				}
				has_war = yes
			}
		}
	}

	naval_raids = {
		intel_source = naval

		available = {
			OR = {
				NOT = {
					has_idea = intervention_local_security
					has_idea = intervention_isolation
				}
				has_war = yes
			}
		}
	}

	#Sending your SF bois in to do their job, unrelated to COIN operations, both in a war and non-war enviornment
	special_forces_raids = {
		intel_source = army

		available = {
			OR = {
				NOT = {
					has_idea = intervention_local_security
					has_idea = intervention_isolation
				}
				has_war = yes
			}
		}
		#Set these to be enabled via SF doctrines
		#Gives players a reason to pick a path on that tree post-rework
	}

	drone_strike_raids = {
		intel_source = army

		available = {
			OR = {
				NOT = {
					has_idea = intervention_local_security
					has_idea = intervention_isolation
				}
				has_war = yes
			}
		}
	}

	nuclear_raids = {
		intel_source = air

		available = {
			OR = {
			 	has_idea = nuclear_power_off
			 	has_idea = nuclear_power_def
			}
			NOT = {
			 	has_idea = no_use_case
			}
			has_war = yes
		}

		free_targeting = yes
	}
}
