ALG_morocco_tensions = {
	allowed = { original_tag = ALG }
	enable = {
		country_exists = SHA
		country_exists = MOR
		SHA = { has_opinion_modifier = SHA_MOR_Occupation }
	}
	abort = {
		OR = {
			NOT = { country_exists = SHA }
			NOT = { country_exists = MOR }
			SHA = { NOT = { has_opinion_modifier = SHA_MOR_Occupation } }
		}
	}

	ai_strategy = { type = antagonize id = "MOR" value = 50 }
	ai_strategy = { type = contain id = "HOR" value = 50 }
}

#Libya supports Algeria
ALG_befriend_libya = {
	allowed = { original_tag = ALG }

	enable = {
		LBA = { has_completed_focus = LBA_support_algeria }
		NOT = { has_war_with = LBA }
	}

	abort_when_not_enabled = yes

	ai_strategy = { type = befriend id = "LBA" value = 25 }
}
#Libya good friends with Algeria
ALG_good_friends_with_libya = {
	allowed = { original_tag = ALG }

	enable = {
		LBA = { has_completed_focus = LBA_approach_algeria }
		NOT = { has_war_with = LBA }
	}

	abort_when_not_enabled = yes

	ai_strategy = { type = befriend id = "LBA" value = 25 }
}